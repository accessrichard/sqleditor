/**
 * Column formatters can be defined
 * by the server database layer and implemented
 * here. If a database encounters a type like
 * a datetime that it can't serialize to 
 * javascript in a user friendly format, 
 * a formatter can be specified via the server
 * and called back here.
 */
define([], function () {

    return { };

});

